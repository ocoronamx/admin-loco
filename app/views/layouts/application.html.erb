<!DOCTYPE html>
<html>
  <head>
    <!-- Caching behavior on a per-page basis: Always fetch a page from server.
         More: https://github.com/turbolinks/turbolinks#opting-out-of-caching -->
    <meta name="turbolinks-cache-control" content="no-cache">
    <title><%= I18n.t(:'app.name') %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body data-scrollbar-auto-hide="n" class="hold-transition sidebar-mini layout-fixed">
      <div class="wrapper">
        <%= render "base/header" %>
        <%= render "base/sidebar" %>

        <div class="content-wrapper">
          <div class="content-header">
            <div class="container-fluid">
              <div class="row mb-2">
                <div class="col-sm-6">
                  <h1 class="m-0 text-dark"><%= @breadcrumb[:title]%></h1>
                </div><!-- /.col -->
                <div class="col-sm-6">
                  <ol class="breadcrumb float-sm-right">
                    <% @breadcrumb[:list].each do |bc| %>
                      <% foo = 'foo' %>
                      <%= link_to bc[:text], bc[:href], class: "breadcrumb-item #{bc[:active]}" %>
<!--                      <li class="breadcrumb-item <%#= bc[:active] %>" href="<%#= bc[:href] %>" <%#= bc[:active] %>>-->
<!--                        <%#= bc[:text] %></li>-->
                    <% end %>
                  </ol>
                </div><!-- /.col -->
              </div><!-- /.row -->
            </div><!-- /.container-fluid -->
          </div>

          <div class="content container-fluid">
            <div class="container-fluid">
              <%= yield %>
            </div>
          </div>
        </div>

        <%= render "base/footer" %>
        <%= render "base/control_sidebar" %>
      </div>
  </body>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', 'data-turbolinks-suppress-warning': true %>
</html>